# .env.example — 提交到 Git，真实值写在 .env（已 gitignore）

# 内容管理后台登录凭证（部署时必须改强密码）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# JWT 签名密钥（部署时必须改为随机长串，可用 `openssl rand -hex 32` 生成）
JWT_SECRET=dev-only-insecure-secret-change-me
