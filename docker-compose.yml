services:
  web:
    build: .
    container_name: xingyu-official
    restart: always
    ports:
      - "127.0.0.1:3010:3000"
    environment:
      - TZ=Asia/Shanghai
    env_file:
      - .env
    volumes:
      - ./server/data:/app/server/data
